version: 2

models:
  - name: metricflow_time_spine
    description: "Time spine required by MetricFlow for semantic layer date calculations."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "One row per calendar day from 2020-01-01 to current date."
        granularity: day